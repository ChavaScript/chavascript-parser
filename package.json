{
  "name": "parser",
  "description": "HavaScript parser",
  "main": "dist/acorn.js",
  "module": "dist/acorn.mjs",
  "version": "1.0.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/havascript/parser.git"
  },
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "rollup -c rollup.config.js",
    "clean-build": "npm run clean && npm run build"
  },
  "devDependencies": {
    "rollup": "^0.45.0",
    "rollup-plugin-buble": "^0.16.0"
  }
}
